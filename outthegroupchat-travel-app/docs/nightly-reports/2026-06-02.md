# Nightly Build Report — 2026-06-02

## Summary
This build **completed the V1 Phase 5 opt-in notification system** (started 2026-06-01, PR #115) and **eliminated the cross-file test flakiness** that was the standing #1 recommendation. Two new dispatch paths landed — the `PER_MEMBER_INTENT` trigger (notify Crew watchers when a flagged member signals an Intent) and the `GROUP_FORMATION` per-user push gate on SubCrew auto-formation — plus a Vitest isolation fix that makes the suite deterministically green (1172/1172), a `profile/page.tsx` refactor under the 600-line ceiling, and +14 tests. All quality gates pass.

## Refactor Phase Status
- **Active phase:** POST_PIVOT_STEADY_STATE (8-phase trip→meetup refactor complete; executing the V1 product vision — intent-to-group loop).
- **V1 Phase 5 (opt-in notifications):** **COMPLETE as of 2026-06-02.** Preferences API + daily prompt + settings UI (PR #115) are now joined by the PER_MEMBER_INTENT trigger and GROUP_FORMATION push wiring (tonight). All three `NotificationPreferenceTrigger` values are live end-to-end.
- **Reference:** `docs/REFACTOR_PLAN.md` §5 (POST_PIVOT_STEADY_STATE note).

## Base-branch decision (noted)
Tonight's branch was based on `nightly/2026-06-01` (**PR #115 = `main` + the Phase 5 foundation**), **not bare `main`**. Rationale: the top recommendation was to *complete* Phase 5, but the notification-preferences API and dispatch libs it builds on exist only in the unmerged PR #115 — building on bare `main` (#92) would have duplicated or conflicted with that work. PR #116 therefore includes #115's 2 commits and **supersedes** it, delivering the complete opt-in notification system as one mergeable unit.

## Pipeline Execution
| Phase | Status | Notes |
|-------|--------|-------|
| Refactor Alignment (P0) | ✅ | Active: POST_PIVOT_STEADY_STATE → completing V1 Phase 5 |
| Git Sync | ✅ | Based on `nightly/2026-06-01` (= main + Phase 5); branch `nightly/2026-06-02` |
| Prev Recommendations | ✅ | Source: 2026-06-01 report (PR #115). Items #1 (flaky) + #2 (Phase 5) executed; #3 partially (1 of 2 >600 files) |
| Codebase Analysis | ✅ | Direct Bash analysis + targeted reads (analysis-agent fallback per known flakiness) |
| Automated Small Tasks | ✅ | Metrics gathered |
| Task Generation | ✅ | Focused 6-task set (4 feature + 2 test), biased to V1 Phase 5 completion |
| Wave 1 (Tests) | ✅ | 2 agents, +14 tests |
| Wave 2 (Features) | ✅ | 4 agents (wave-reversed: features before tests, new libs) |
| Wave 3 (Shared Files) | ✅ | 1 agent — setup.ts unchanged, 5 docs updated |
| Validation | ✅ | Build/Lint/TSC/Prisma pass; tests 1172/1172 deterministic |
| Git & PR | ✅ | PR #116 |
| Notion Report | ⚠️ | Local fallback (this file) |

## Wave 2: Features & Refactors
1. **[F1]** PER_MEMBER_INTENT trigger — success — new `lib/notifications/per-member-intent.ts` + wired into `POST /api/intents`. ACCEPTED-Crew guard; SYSTEM notif + `data.kind:'PER_MEMBER_INTENT'`; non-fatal. Topic human-label field confirmed as `displayName`.
2. **[F2]** GROUP_FORMATION push gating — success — `lib/subcrew/try-form.ts` additive push (`broadcastToUser`,`'subcrew:formed'`) gated per-seed on the GROUP_FORMATION toggle. Existing SUBCREW_FORMED in-app notif untouched.
3. **[F3]** Flaky cross-file mock-isolation fix — success — `vitest.config.ts`: `clearMocks` + `fileParallelism:false` + `isolate:true`. Rejected `mockReset`/`restoreMocks`/`unstubGlobals` (each caused 11 net-new `geocoding.test.ts` failures via destroyed module-scope `vi.stubGlobal`). Two consecutive full runs: 0 failures.
4. **[F5]** Refactor `profile/page.tsx` 623→559 — success — extracted `components/profile/ProfileCheckinsSection.tsx`.

## Wave 1: Test Writers (+14)
1. **[T1]** `__tests__/lib/per-member-intent.test.ts` — success — 10 tests (no-match short-circuit, eligible Crew watcher, author exclusion, Crew-guard exclusion, per-row swallow + counts).
2. **[T2]** `__tests__/lib/subcrew-group-formation-push.test.ts` — success — 4 tests (both/one/none enabled, findMany-rejection non-fatal).

## Wave 3: Shared File Updates
- `setup.ts`: **no change** — all dispatch-path delegates (`notificationPreference.findMany`, `crew.findMany`, `topic.findUnique`, `user.findUnique`, `notification.create`) already mocked.
- `API_STATUS.md`, `CURRENT_SPRINT.md`, `CODEMAP.md`, `LAUNCH_CHECKLIST.md`, `REFACTOR_PLAN.md`: updated to 2026-06-02; Phase 5 marked complete; opt-in trigger items checked off.

## Tasks Failed
None.

## Codebase Health Metrics
| Metric | Previous | Current | Trend |
|--------|----------|---------|-------|
| Tests | 1158 | 1172 | ↑ |
| Test files (vitest) | 69 | 71 | ↑ |
| any types | 4 | 4 | → |
| console.* | 0 | 0 | → |
| TODO/FIXME | 0 | 0 | → |
| Files >600 lines (prod) | 2 | 1 | ↓ (good) |
| API route.ts files | 74 | 74 | → |

## Build & Test Results
- **Build:** PASS
- **Lint:** 0 warnings, 0 errors
- **TSC:** 0 errors
- **Prisma:** valid
- **Tests:** 1172 / 1172 pass — **deterministic** (serial pool via `fileParallelism:false`). The intermittent 2–4 cross-file failures reported on 2026-06-01 are resolved.

## Security Findings
Clean. New code adds no routes; both dispatch paths run server-side under the existing authenticated `POST /api/intents` flow, are Zod-gated upstream, and are wrapped non-fatally. No new external input surface, no email exposure, no hardcoded secrets.

## Next Day Recommendations
1. **[HIGH] Drain the nightly-PR backlog.** Six PRs are unmerged (#110–#115, with #116 now superseding #115). #110 is itself a 17-PR consolidation. This is the dominant project risk — every nightly stacks more divergence on stale `main`. Run a dedicated consolidation session: close #115 in favor of #116, then sequence-merge or re-consolidate #110–#114 onto `main`. No new feature nightlies until `main` reflects current reality.
2. **[MEDIUM] Refactor the last >600-line file:** `components/feed/RichFeedItem.tsx` (717). A prior refactor exists in stale PR #56 — redo on current `main` (extract `FeedItemHeader`/`FeedItemActions`/`FeedItemLegacyCards` as that PR did) rather than resurrecting the branch.
3. **[MEDIUM] Production launch blockers (env, not code):** set Sentry DSN, Pusher env vars (GROUP_FORMATION push is now wired but no-ops without them), and verify the Resend domain in Vercel.
4. **[MEDIUM] Defense-in-depth test isolation:** so file-parallelism can eventually be re-enabled (faster CI), convert module-scope `vi.stubGlobal('fetch', …)` in `geocoding.test.ts` to a `beforeEach` stub, and re-arm shared module mocks (e.g. pusher `triggerCheckinEvent`) with `mockResolvedValueOnce`/`beforeEach`. Then `fileParallelism:false` can be lifted.
5. **[LOW] V1 heatmap sub-questions R22/R24:** venue-marker zoom threshold and event-context detection for Crew-anchor selection (deferred from Phase 4).

These recommendations will be picked up by the next nightly build.

## Errors & Warnings
- LF→CRLF warnings on staged files (cosmetic, Windows line-ending normalization).
- Analysis Phase ran via direct Bash + targeted file reads rather than 6 background agents (per the documented tendency of background analysis agents to not return) — coverage was equivalent for this focused scope.

---
*Generated by OutTheGroupchat Nightly Build Coordinator v2 — run 2026-06-02T22:38-05:00*
