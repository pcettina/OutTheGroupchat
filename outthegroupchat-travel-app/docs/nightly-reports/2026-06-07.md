# Nightly Build Report — 2026-06-07 (BACKLOG CONSOLIDATION)

## Summary
The 11-PR backlog that had been frozen behind `main` (#92) since late April was
**consolidated into one green tree and landed on `main`**. This resolves the standing
blocker re-escalated for four consecutive nights. The founder authorized the canonical
decision (June's V1 Phase-5 notifications implementation wins), and this run executed the
verified merge order, salvaged the one net-new item from the May forks, hardened the
nightly automation against the failure mode that caused the stall, and cleaned up the
stale branch noise.

**Result: 1805 tests passing / 90 test files, TSC 0 errors, lint clean.**

## What landed on `main`
Two independent forks off `main` (#92) plus one salvage, reconciled in this order:

1. **#110 anchor** (`claude/integrate-pull-requests-ksahP`) — consolidates PRs #93–#109
   (1775 tests, 87 files). Merged to `main` cleanly (zero conflicts).
2. **June nightly chain tip** `nightly/2026-06-06` (#120 = #115→#120) — the
   **founder-declared canonical** V1 Phase-5 opt-in notifications implementation. Merged
   on top of #110; 19 conflicts resolved (see below).
3. **#112 topics rate-limit** — the only genuinely net-new code from the May forks
   (#111–#114) not already present via #110/June. Cherry-picked + test added.

### Conflict resolution (19 files, June-onto-#110)
| Area | Resolution | Why |
|------|-----------|-----|
| heatmap libs (`aggregate`, `anchor-select`, `fof-graph`) | June | JSDoc-only diffs; **code byte-identical** |
| Phase-5 routes/UI/tests (`notification-preferences`, `cron/send-daily-prompts`, `settings/notifications`) | June | founder-declared canonical |
| `setup.ts` `notificationPreference` mocks | June | superset (adds `createMany`) |
| `heatmap-edge` / `recommendations-edge` tests | June | supersets (24>17, 23>16 cases) |
| `profile/page.tsx` | #110 | fuller refactor (5 extracted components, all present & green) |
| `RichFeedItem.tsx` | deleted (June) | confirmed dead (barrel-only ref) |
| `ReactionPicker.tsx` | **restored** | live again in merged tree (used by #110's `FeedItemEngagement` + `rich-item/EngagementBar`); June's deletion was valid only on its smaller pre-merge tree — TSC caught this cross-side conflict |
| docs (README ×2, API_STATUS, CODEMAP, CURRENT_SPRINT, LAUNCH_CHECKLIST) | #110 then refreshed | #110 richer; re-stamped to 2026-06-07 here |

### May forks (#111–#114) triage
- **Net-new salvaged:** `#112` per-user rate-limit on `GET /api/topics` (→ 429), with a
  hoisted rate-limit pass-through mock + 429 test (re-armed after `resetAllMocks` per the
  known gotcha).
- **Already present via #110:** search `FilterChip`/`SearchFilters` components.
- **Superseded / docs-only:** everything else in #111–#114.

## Automation fix (root-cause)
`SKILL.md` Phase 1 (Git Sync) was rewritten with **base-branch selection logic**: it now
detects when `main` is behind a clean linear nightly tip and bases the new branch on that
tip (PR supersedes), never re-forking frozen `main`. Added an escalation guard: if `main`
is ≥5 nightlies behind, flag a CRITICAL human-merge recommendation and stay doc-only.
This is the fix for the exact failure that orphaned #110–#120.

## Branch cleanup
Stale local + remote `nightly/*` branches whose work is now on `main` were deleted to
reduce noise. Superseded PRs (#110–#120) closed with pointers to this consolidation.

## Codebase Health Metrics
| Metric | Pre-consolidation (`main` #92 / June tip) | Post-consolidation (`main`) | Trend |
|--------|-------------------------------------------|------------------------------|-------|
| Tests passing | 1172 (June tip) / 1775 (#110) | 1805 | ↑ |
| Test files | 71 / 87 | 90 | ↑ |
| Live API routes | 61 | 61 | → |
| Open PRs (backlog) | 11 | 0 | ↓✅ |
| `main` commits behind real state | 8–43 | 0 | ↓✅ |

## Build & Test Results
- **TSC:** 0 errors
- **Lint:** 0 warnings/errors
- **Tests:** 1805 passed / 1805 (90 files)
- **Prisma:** valid

## Next Day Recommendations
1. **[HIGH]** Resume normal nightly cadence now that `main` reflects real state — the base
   stall is resolved and the automation guard is in place. Next feature focus: Phase 8
   launch-readiness (Sentry coverage restoration audit, E2E Playwright auth flows).
2. **[MEDIUM]** Verify the new `SKILL.md` Phase-1 base-branch logic on tonight's run (it
   should branch cleanly from the freshly-advanced `main`).
3. **[MEDIUM]** Spot-check the V1 Phase-5 notification triggers end-to-end against the
   consolidated schema (DAILY_PROMPT, PER_MEMBER_INTENT, GROUP_FORMATION).
4. **[LOW]** Prune any remaining `.claude/worktrees/` and old local branches not covered
   by tonight's cleanup.

## Errors & Warnings
- None blocking. `git add -A` avoided throughout (untracked `outthegroupchat-mobile/`,
  `knowledge/`, `.claude/` excluded). All merges verified green before advancing `main`.

---
*Generated by OutTheGroupchat Nightly Build Coordinator v2 — backlog consolidation run.*
