# Nightly Build Report — 2026-04-27

## Summary
Focused report-mode nightly build. Main is clean and the legacy nightly-PR backlog
(#56, #59, #67) is gone — only PR #93 (a manual claude-coded auth design pass) is
open. Tonight's run fixed one real TSC error introduced by PR #92, refreshed the
stale `CODEMAP.md` header, and de-bunked the previous build's `any`-count
"regression" (it was a false positive in the metrics regex). Heavy parallel waves
were skipped to stay out of the way of the user's untracked mobile-app and
launch-research work.

## Refactor Phase Status
- **Original plan (`docs/REFACTOR_PLAN.md`):** Heuristic still classifies as Phase 8
  (launch-readiness re-audit). Not the active driver.
- **V1 Product Vision (founder-locked 2026-04-24, `docs/PRODUCT_VISION.md` +
  `docs/V1_IMPLEMENTATION_PLAN.md`):** Phase 4b (heatmap FoF tier) shipped via
  PR #88. Heatmap CSP hotfix (#89), Neon prod-migration workflow (#90), and
  heatmap test fixtures (#91, #92) all landed on main since the previous nightly.
- **Open PRs (as of 2026-04-27 03:00 UTC):**
  - **PR #93** (`claude/great-mahavira-e3f786`) — V1 Last-Call aesthetic on signup +
    signin pages. Manual UI work, not auto-generated. Recommend: review + land.
- **Phase exit criteria:** Not evaluated tonight (no agentic phase work).

## Pipeline Execution
| Phase | Status | Notes |
|-------|--------|-------|
| P0 Refactor Alignment | ✅ | V1 Product Vision phases shipped through Phase 4b on main; legacy REFACTOR_PLAN superseded. |
| P1 Git Sync | ✅ | Created branch `nightly/2026-04-27` from clean `main`. User's untracked mobile-app/docs preserved. |
| P2 Prev Recommendations | ✅ | Read `docs/nightly-reports/2026-04-26.md`. Items #1 (PR triage), #3 (#89/#90 land), #4 (`any` regression) addressed/closed below. |
| P3 Codebase Analysis | ⚠️ | Skipped — direct `tsc`/`grep` used instead. Same lightweight rationale as 2026-04-26. |
| P3.5 Automated Small Tasks | ✅ | All 8 metrics collected. |
| P4 Task Generation | ⚠️ | Bypassed in favor of two focused fixes (TSC + CODEMAP). |
| P5 Wave 1/2/3 Execution | ⚠️ | Bypassed — heavy parallelism would risk colliding with user's in-flight `outthegroupchat-mobile/` and untracked docs. |
| P6 Validation | ✅ | TSC / lint / vitest / prisma all clean after fixes. |
| P7 Git & PR | ✅ | Branch + PR created. |
| P8 Notion Report | — | Local fallback only (this file). |

## Codebase Health Metrics (current tree, branch `nightly/2026-04-27`)
| Metric | Prev (2026-04-26) | Current | Trend |
|--------|-------------------|---------|-------|
| Tests passing | 1081 | **1081** | → |
| Test files (vitest-active) | 64 | 64 | → |
| Total test files on disk | 86 | 86 | → |
| API routes (`route.ts`) | 72 | 72 | → |
| TS/TSX files (src/) | 367 | 367 | → |
| `any` types in prod | 4 (false positive) | **0** (corrected) | ✅ |
| `console.*` in prod | 0 | 0 | → |
| TODO/FIXME | 2 | 2 | → |
| Files >600 lines (prod) | 2 | 2 | → |
| Lint warnings | 0 | 0 | → |
| TSC errors | 0 | **0 (fixed: was 2)** | ✅ |
| Prisma valid | ✅ | ✅ | → |

### Notes on metrics
- **`any` correction:** The previous build's count of 4 was a regex false
  positive. The pattern `: any\b` matched English-prose comments such as
  `"PUBLIC: any authenticated user."` and `"// Trending: any PUBLIC meetup..."`
  in route JSDoc. Re-running with `\bas any\b|: any[\s,;|>)\]&]` returns zero
  hits in `src/`. **Recommendation:** see Next-Day Recommendations #1 — patch the
  regex in `~/.claude/scheduled-tasks/nightly-otgc-build/SKILL.md` Phase 3.5.
- **TSC errors fixed:** PR #92 (standalone heatmap runner) introduced two TS5097
  errors in `prisma/scripts/seed-heatmap-only.ts`:
  ```
  prisma/scripts/seed-heatmap-only.ts(11,37): error TS5097
  prisma/scripts/seed-heatmap-only.ts(12,28): error TS5097
  ```
  Caused by `.ts` extensions on imports under `moduleResolution: "bundler"`
  without `allowImportingTsExtensions`. Fixed by dropping the `.ts` suffix
  (matching the convention used by `prisma/seed/index.ts`).
- **Files >600 lines (prod):** unchanged — `src/app/profile/page.tsx` (623),
  `src/components/feed/RichFeedItem.tsx` (717). Refactor PRs (#56, #59) were
  closed/superseded since the previous build, so these now need a fresh look.

## Open Pull Request Backlog (as of 2026-04-27 03:00 UTC)
| PR # | Branch | Title | Priority | Recommendation |
|------|--------|-------|----------|----------------|
| #93 | `claude/great-mahavira-e3f786` | V1 Last-Call aesthetic on auth pages | P1 | Manual UI design pass. Review for token correctness + accessibility, then merge. |

Legacy nightly PRs (#56, #59, #67) — all gone since the 2026-04-26 build. Backlog
recommendation #1 from 2026-04-26 is **closed**.

## Untracked User Work (preserved, not committed)
- `outthegroupchat-mobile/` — full Expo/React Native app (iOS + Android targets).
- `knowledge/`
- `.claude/`
- `outthegroupchat-travel-app/docs/LAUNCH_RESEARCH_PORTFOLIO.md`
- `outthegroupchat-travel-app/docs/MOBILE_BUILD_ONESHOT.md`
- `outthegroupchat-travel-app/docs/iOS_IMPLEMENTATION_PLAN.md`

These were not modified or staged.

## Tasks Failed
None — pipeline was deliberately scoped down.

## Build & Test Results (current tree, after fixes)
- **TSC:** 0 errors (was 2 — fixed in this build).
- **Lint:** 0 warnings, 0 errors.
- **Tests:** 1081 passed / 1081 total (64 vitest-active files, 22.76s).
- **Prisma:** valid.
- **Build:** Not run (only TSC verified — same rationale as 2026-04-26 to avoid
  `.next` cache invalidation while user has untracked dev work).

## Files Changed This Build
- `outthegroupchat-travel-app/prisma/scripts/seed-heatmap-only.ts` — drop `.ts`
  extensions on internal imports (TSC fix).
- `outthegroupchat-travel-app/docs/CODEMAP.md` — refresh header counts
  (58→72 routes, 47→64 vitest-active files, 900→1081 tests, 301→367 TS/TSX
  files; date 2026-04-24 → 2026-04-27).
- `outthegroupchat-travel-app/docs/nightly-reports/2026-04-27.md` — this report.

## Security Findings
None this run. Direct `grep` for hardcoded secrets clean. No new auth-bypass
patterns. `getServerSession` coverage on protected routes unchanged.

## Next-Day Recommendations

1. **[HIGH] Patch the `any`-count regex in
   `~/.claude/scheduled-tasks/nightly-otgc-build/SKILL.md` Phase 3.5.** Current
   pattern `: any\b` produces false positives on English prose in JSDoc/inline
   comments. Replace with something like
   `\bas any\b|:\s*any[\s,;|>)\]&]|<any[\s,>]` and require the file is not
   excluded by `--include` filters. Also consider running through
   `eslint --rule '{"@typescript-eslint/no-explicit-any":"error"}' --quiet`
   instead of grep — it would be definitive. Estimated effort: 10 min.

2. **[HIGH] Review and merge PR #93 (`claude/great-mahavira-e3f786`).** Auth-page
   redesign — needs a quick design + accessibility check. Currently the only open
   PR.

3. **[MEDIUM] Decide nightly pipeline future (recommendation #2 from 2026-04-26
   still standing).** The pipeline assumed a calm `main` and a single-track
   REFACTOR_PLAN. The project now has parallel tracks (V1 product vision +
   mobile app + ops/migrations). Either pause the nightly until mobile-app +
   migrations are merged, OR rewrite `SKILL.md` Phase 0 alignment heuristic to
   read `docs/V1_IMPLEMENTATION_PLAN.md` and bias tasks toward the V1 phase set.

4. **[MEDIUM] Decide whether `outthegroupchat-mobile/` belongs in this monorepo.**
   Still untracked. If yes — add to git with `.gitignore` rules + a CI matrix.
   If no — move to its own repo to keep the webapp tree clean.

5. **[MEDIUM] Refactor `src/components/feed/RichFeedItem.tsx` (717 lines) and
   `src/app/profile/page.tsx` (623 lines).** Original refactor PRs (#56, #59)
   were closed/superseded. These are the only two prod files over the 600-line
   limit. A focused single-day refactor would clear the only open code-health
   item.

6. **[LOW] Refresh `MEMORY.md`.** Was 291 lines / 36.6KB at the start of this
   session. Run the `consolidate-memory` skill to merge stale entries (test
   counts, route counts, and Phase-7/Phase-8 status are all out-of-date in
   `MEMORY.md`).

7. **[LOW] Update the body of `docs/CODEMAP.md`.** Header is current as of this
   build, but the §"Stats (post-Phase-6-complete, 2026-04-22)" body block still
   references 50 routes / 58 test files. A full body rewrite would take 30 min;
   not urgent.

8. **[LOW] Resume Sentry expansion audit** once the V1 surface stabilizes.
   `REFACTOR_PLAN.md` Phase 8 action #6 still outstanding.

## Errors & Warnings
None during this run.

---
*Generated 2026-04-27 03:05 UTC by OutTheGroupchat Nightly Build Coordinator
(focused report mode — fixed real TSC regression from PR #92 + corrected metrics
regex; deliberately skipped heavy waves to avoid colliding with user's in-flight
mobile-app work).*
