# Nightly Build Report — 2026-07-13

**Status:** ✅ Success · **PR:** [#144](https://github.com/pcettina/OutTheGroupchat/pull/144) · **Branch:** `nightly/2026-07-13`

## Summary
Second consecutive normal PLAN execution after the escalation hold cleared. Executed **BUILD_PLAN Day 4 — "Per-relationship privacy defaults"** (3/3 tasks) — the load-bearing V1 gap where `CrewRelationshipSetting` was *read* by the heatmap aggregator (`lib/heatmap/aggregate.ts`) but had no write path, leaving R4/R20 (per-Crew-member location privacy) unreachable. Shipped the write route, a "Manage who sees your location" UI on a new `settings/privacy/relationships` subroute, and 16 tests. All validation green on the first full run — no revert cycle. `nightly/2026-07-12` was a missed night; the queue advanced to the next unstarted Day (catch-up), so Day 4 (labeled `nightly/2026-07-03`) ran on branch `nightly/2026-07-13`.

## Build-Plan Status
- **Task source:** PLAN
- **Active Day:** Day 4 — labeled `nightly/2026-07-03`, run on catch-up branch `nightly/2026-07-13`
- **Day goal:** A user can open "Manage who sees my location," set BLOCK/DYNAMIC_CELL/HIDDEN + KNOWN/ANONYMOUS/CREW_ANCHORED per Crew member, and the aggregator honors it.
- **Progress tonight:** 3/3 tasks (L1 + M1 + T1) landed green
- **Day status after tonight:** ✅ COMPLETE
- **Days remaining in plan:** 10 (Days 5–14)
- **Reference:** `docs/BUILD_PLAN.md` §2

## Escalation Guard — CLEARED
`main` (`d2a18ed`) measured **3 nightlies behind** the clean linear tip `origin/nightly/2026-07-11` (below the ≥5 doc-only threshold), so feature fan-out proceeded. Per the base-branch rule, tonight's branch was based on `origin/nightly/2026-07-11` (the clean linear tip it supersedes), **not** bare `main`. `main` still advances only by manual merge / the not-yet-installed auto-ff Action — see recommendation #1.

## Pipeline Execution
| Phase | Status | Notes |
|-------|--------|-------|
| Build-Plan Alignment (P0) | ✅ | Source: PLAN Day 4 |
| Git Sync | ✅ | main 3 behind nightly/2026-07-11 tip; based branch on that tip (supersedes) |
| Prev Recommendations | ✅ | Source: local (2026-07-11) |
| Codebase Analysis | ✅ | Metrics via Phase 3.5 + targeted recon (PLAN mode) |
| Automated Small Tasks | ✅ | 8 metrics captured |
| Task Generation | ✅ | Day 4 tasks verbatim |
| Wave 2 (Features) | ✅ | 2 agents (L1, M1); M1 re-run once after a stream-watchdog stall (no files lost) |
| Wave 1 (Tests) | ✅ | 1 agent (T1), run after impl so tests hit real code |
| Wave 3 (Shared Files) | ✅ | 1 agent |
| Validation | ✅ | build+lint+test+prisma+tsc all green, first pass (no reverts) |
| Git & PR | ✅ | PR #144 |
| Notion Report | ⚠️ | Notion MCP unauthorized in non-interactive run → local fallback (this file) |

## Wave 2: Features
1. **[L1] Write route** — ✅ — *created* `src/app/api/users/relationship-settings/route.ts`. GET lists one entry per accepted Crew member (undirected edge resolved + de-duped), left-joined against stored `CrewRelationshipSetting` rows, defaulting **BLOCK/KNOWN** when absent, sorted by name. PATCH validates `targetId !== self` (400) and accepted-Crew membership (403 "Not in your Crew") before upserting on the `viewerId_targetId` unique key. Mirrors `notification-preferences/route.ts` exactly: Zod `nativeEnum`, `getServerSession` auth (401), rate-limit (429 + headers), invalid-JSON (400), Sentry `captureException` + `apiLogger` in catches (500).
2. **[M1] Management UI** — ✅ — *created* `src/components/privacy/RelationshipSettingsList.tsx` (client) + `src/app/settings/privacy/relationships/page.tsx` (new server subroute); *modified* `src/app/settings/privacy/page.tsx` (added one link; existing check-in-visibility page otherwise untouched — no collision). Per-member granularity + identity radio-card groups reuse the option label/hint copy from `PrivacyPickerModal`; optimistic PATCH sends both current values with revert-on-error + inline error + "Saved" affordance; loading/error/empty states; static R14 helper on ANONYMOUS ("Only shown when at least 3 people share a cell"); `next/image` avatar (matches app convention).

## Wave 1: Test Writers
1. **[T1] Relationship-settings tests** — ✅ — *created* `src/__tests__/api/relationship-settings.test.ts` — **16 tests**. GET: 401, BLOCK/KNOWN defaults, stored-row override, partner resolution for both edge directions, empty crew, 429, 500+captureException. PATCH: 401, invalid JSON, bad enum, missing targetId, self-target 400, not-in-Crew 403, 200 upsert (asserts `where: viewerId_targetId` + create/update args + response shape), 429, 500+captureException. **No new `setup.ts` mocks needed** — `crew` and `crewRelationshipSetting` delegates were already mocked.

## Wave 3: Shared File Updates
- `setup.ts`: no change (no new mocks required this run)
- `BUILD_PLAN.md`: Day 4 → ✅ COMPLETE (#144, 2026-07-13) — only that Status line changed
- `API_STATUS.md`: added GET/PATCH `/api/users/relationship-settings`; live routes 62; Last Updated 2026-07-13
- `CURRENT_SPRINT.md`: Completed 2026-07-13 (Day 4) section; tests 1909
- `CODEMAP.md`: stats refresh (62 live routes, 119 test files, 1909 tests, 416 TS/TSX); Last updated 2026-07-13
- `LAUNCH_CHECKLIST.md`: checked off per-relationship location privacy (R4/R20); Last Updated 2026-07-13

## Tasks Failed
None. (M1's first agent run hit a stream-watchdog stall before writing any file; it was re-launched fresh and completed cleanly — no partial/lost work.)

## Codebase Health Metrics
| Metric | Previous (2026-07-11) | Current | Trend |
|--------|----------------------|---------|-------|
| Tests (passing) | 1893 | 1909 | ↑ +16 |
| Test files | 118 | 119 | ↑ +1 |
| Live API routes (excl `_archive`) | 61 | 62 | ↑ +1 |
| any types | 0 | 0 | → |
| console.* | 0 | 0 | → |
| TODO/FIXME | 0 | 0 | → |
| Files >600 lines (prod) | 0 | 0 | → |
| TS/TSX files | 414 | 416 | ↑ +2 |

## Build & Test Results
- **Build:** PASS (exit 0; `/settings/privacy/relationships` present in route table, existing `/settings/privacy` intact)
- **Lint:** 0 warnings, 0 errors (one initial `<img>` warning in the new UI was fixed to `next/image` before commit)
- **Tests:** 1909 passed / 1909 (97 vitest files)
- **Prisma:** valid
- **tsc:** 0 errors

## Security Findings
No new findings. The new route reuses `getServerSession()` + Zod `nativeEnum` validation and adds a meaningful authz gate: PATCH refuses to write a privacy row for any user who is not an accepted Crew member of the caller (403), and refuses self-targeting (400) — preventing enumeration/writes against arbitrary user IDs. Sentry `captureException` on both catch paths. No secrets committed (pre-commit sensitive-file scan clean).

## Next Day Recommendations
1. **[HIGH / PROCESS — INSTALL THE AUTO-FF ACTION]** Still not installed; the nightly agent's OAuth token lacks the `workflow` scope so it cannot self-install. A human runs once: `git mv outthegroupchat-travel-app/docs/ops/auto-ff-main.yml .github/workflows/auto-ff-main.yml && git commit -m "ci: auto-ff main from latest green nightly" && git push` (or create it via the GitHub web UI, which carries `workflow` scope). Without it, `main` only advances by manual PR merges — the recurring stall risk. Merging PRs #143 and #144 keeps the chain linear in the meantime.
2. **[HIGH / PLAN]** Tomorrow's Day is **Day 5 — Trust & Safety I: block a user** (`UserBlock` model + block/unblock routes + **four-surface** enforcement: crew list, feed, heatmap aggregation, check-in feed). Per §0/§104 this Day is explicitly carry-over-eligible — if all four enforcement surfaces don't land green together, ship the model+routes+landed surfaces, mark Day 5 🟡 IN PROGRESS with a precise carry-over, and finish before Day 6. Adds an additive `UserBlock` migration (Neon per-PR branch — remember failed migrations stick until close+reopen).
3. **[HIGH / OPS]** Set **`SENTRY_DSN`** + `NEXT_PUBLIC_SENTRY_DSN` in Vercel prod — tonight's `captureException` calls (and all prior instrumentation) ingest zero events without it. Highest-leverage launch action.
4. **[MEDIUM / OPS]** Remaining env-only launch gates: **Pusher** prod vars, **`GOOGLE_PLACES_API_KEY`**, **Resend** domain verification, `NEXTAUTH_SECRET` audit, uptime monitor on `/api/health`. None code-fixable; several gate the *value* of shipped Days (hotness, real-time).
5. **[LOW / FOLLOW-UP]** `identityMode` written by tonight's route is **read-side-inert today** — `aggregate.ts` only acts on `granularityMode === HIDDEN` (BLOCK vs DYNAMIC_CELL is not yet differentiated, and identity is taken from each `HeatmapContribution.identityMode` at write time). A future Day should wire the stored per-relationship `identityMode`/`DYNAMIC_CELL` into the aggregate read path so the setting fully takes effect end-to-end.

These recommendations will be automatically picked up by tomorrow's nightly build.

## Errors & Warnings
- Notion MCP required OAuth auth unavailable in this non-interactive run → report written locally (this file), per Phase 8 fallback. Not a failure.
- M1's first agent run stalled on a stream watchdog (600s no-progress) before writing any file; re-launched and completed cleanly. No lost work.
- Foreground `npm run build` / `vitest run` exceed the 2-minute shell wall; both were run detached and polled to completion (build exit 0, tests exit 0). Not a build issue.
- Non-fatal build noise: offline lockfile-patch `fetch failed` (cert) + stale `caniuse-lite` browserslist warning — neither affects output.

---
*Generated by OutTheGroupchat Nightly Build Coordinator v2 — plan-driven (BUILD_PLAN.md Day 4)*
