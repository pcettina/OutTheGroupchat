# Nightly Build Report — 2026-06-01

## Summary
This build opened **V1 Phase 5 — the opt-in notification system**. It shipped the notification-preferences API (GET/PATCH), a Vercel Hobby-tier-compliant daily-prompt cron with its dispatch lib, and a `/settings/notifications` UI, alongside +77 tests and a cleanup pass (TODOs cleared to 0, a seed-script TSC fix, and JSDoc on the V1 heatmap/subcrew privacy libs). All quality gates pass; the full suite is green (1158/1158) when run sequentially.

## Refactor Phase Status
- **Active phase:** POST_PIVOT_STEADY_STATE
- **Objective:** The 8-phase trip→meetup refactor is complete; work executes the V1 product vision (intent-to-group loop) on the stable meetup/crew/check-in base.
- **Progress tonight:** Began V1 Phase 5 (opt-in notifications) — notification-preferences API, daily-prompt cron + lib, and settings UI landed. Per-member-intent trigger dispatch remains for a follow-up build.
- **Phase exit:** Not yet — Phase 5 partially delivered (preferences + daily prompt + UI). Remaining: per-member Intent trigger on `Intent.create`, and GROUP_FORMATION push wiring.
- **Reference:** `docs/REFACTOR_PLAN.md` §5 (POST_PIVOT_STEADY_STATE note appended)

## Pipeline Execution
| Phase | Status | Notes |
|-------|--------|-------|
| Refactor Alignment (P0) | ✅ | Active: POST_PIVOT_STEADY_STATE → V1 Phase 5 |
| Git Sync | ⚠️ | `git pull` blocked by local SSL cert error; proceeded on local main (== origin/main fd3e5ae). Push later succeeded via schannel backend. |
| Prev Recommendations | ⚠️ | Latest on-disk report 2026-04-24 — stale (pre-dates V1 #76–#92). Used as source per fallback chain; items already resolved. |
| Codebase Analysis | ✅ | 6 agents |
| Automated Small Tasks | ✅ | Metrics gathered |
| Task Generation | ✅ | 10 agentic tasks (biased to V1 Phase 5) |
| Wave 1 (Tests) | ✅ | 4 agents, +77 tests |
| Wave 2 (Features) | ✅ | 6 agents |
| Wave 3 (Shared Files) | ✅ | 1 agent |
| Validation | ✅ | Build/Lint/TSC/Prisma pass; tests 1158/1158 sequential |
| Git & PR | ✅ | PR #115 |
| Notion Report | see below | local fallback written |

## Wave 1: Test Writers
1. [T1] notification-preferences route tests — success — 16 tests
2. [T2] send-daily-prompts cron + daily-prompt lib tests — success — 14 tests
3. [T3] heatmap edge cases — success — 24 tests
4. [T4] recommendations edge cases — success — 23 tests

## Wave 2: Features & Refactors
1. [F1] notification-preferences API (GET/PATCH, Zod, auth, rate-limit, Sentry) — success
2. [F2] daily-prompt cron + `lib/notifications/daily-prompt.ts`; `vercel.json` daily `0 13 * * *` — success
3. [F3] `/settings/notifications` page + `NotificationPreferencesForm` — success
4. [F4] fixed 2 TS5097 errors in `prisma/scripts/seed-heatmap-only.ts` — success
5. [F5] resolved 2 TODOs in `checkins/page.tsx` (wired CheckInButton) — success
6. [F6] JSDoc on 4 V1 heatmap/subcrew privacy libs — success

## Wave 3: Shared File Updates
- `setup.ts`: notificationPreference mock (added at coordinator level)
- `API_STATUS.md`: V1 route surface documented, route count → 61
- `CODEMAP.md` / `CURRENT_SPRINT.md`: ~1158 tests, 69 files, 61 routes
- `LAUNCH_CHECKLIST.md`: opt-in notification consent control checked off
- `REFACTOR_PLAN.md`: POST_PIVOT_STEADY_STATE note
- `README.md`: stale AI-route reference removed, counts refreshed

## Tasks Failed
None.

## Codebase Health Metrics
| Metric | Previous | Current | Trend |
|--------|----------|---------|-------|
| Tests | 1081 | 1158 | ↑ |
| Test files | 64 | 69 | ↑ |
| API routes (live) | 59 | 61 | ↑ |
| any types | 4 | 4 | → |
| console.* | 0 | 0 | → |
| TODO/FIXME | 2 | 0 | ↓ (good) |
| Files >600 lines | 2 | 2 | → |

## Build & Test Results
- **Build:** PASS
- **Lint:** 0 warnings, 0 errors
- **TSC:** 0 errors
- **Prisma:** valid
- **Tests:** 1158/1158 pass (sequential / deterministic). Under the parallel pool, 2–3 tests intermittently fail across varying files (checkins-pusher, privacy-settings) — confirmed **pre-existing cross-file mock-isolation flakiness** (each suspect file passes 11/11 in isolation; the pre-change baseline also flaked with different files).

## Security Findings
Clean. All live API routes enforce auth (getServerSession / N8N x-api-key / cron secret). All request-body parsing is Zod-validated. No cross-user email exposure (search route strips email). Only `$queryRaw` is `SELECT 1` (health). No hardcoded secrets in `src/`.

## Next Day Recommendations
1. **[HIGH]** Fix the test suite's cross-file mock-isolation flakiness. Root cause is shared module-mock state (e.g. `triggerCheckinEvent`, rate-limit, getServerSession) leaking across files under the parallel pool. Audit `vi.mock` factories using `mockResolvedValue` (vs `Once`) and ensure `vitest.config` has `restoreMocks`/`clearMocks` + per-file resets; consider `isolate: true`. Until fixed, CI should run `--no-file-parallelism` or retry-on-flaky.
2. **[HIGH]** Complete V1 Phase 5: wire the **PER_MEMBER_INTENT** trigger — on `Intent.create`, notify users who flagged the author in `NotificationPreference.perMemberTargets` ("[Name] is up for [topic]"). Then wire **GROUP_FORMATION** push (respect the per-user toggle) into `lib/subcrew/try-form.ts`.
3. **[MEDIUM]** Refactor the two files >600 lines (`RichFeedItem.tsx` 717, `profile/page.tsx` 623). Refactors exist in stale open PRs (#56/#59) but are unmerged; either land them or redo on current main.
4. **[MEDIUM]** Production blockers for launch: set Sentry DSN, Pusher env vars, and verify the Resend domain in Vercel. These gate real-time + monitoring + email deliverability.
5. **[LOW]** Resolve the 2 deferred V1 heatmap sub-questions: R22 (zoom threshold for venue-marker rendering) and R24 (event-context detection for Crew-anchor selection).

These recommendations will be picked up by the next nightly build.

## Errors & Warnings
- `git pull origin main` and the initial `git push` failed with `SSL certificate ... unable to get local issuer certificate (20)`. Worked around the push with `git -c http.sslBackend=schannel` (Windows native cert store). The local SSL cert chain for the HTTPS remote should be fixed on this machine (set `git config --global http.sslBackend schannel`) to avoid recurring nightly friction.
- LF→CRLF warnings on staged files (cosmetic, Windows line-ending normalization).

---
*Generated by OutTheGroupchat Nightly Build Coordinator v2 — pipeline run 2026-06-01*
