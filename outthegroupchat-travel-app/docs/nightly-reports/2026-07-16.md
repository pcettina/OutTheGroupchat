# Nightly Build Report — 2026-07-16

## Summary
Executed **BUILD_PLAN Day 5 — Trust & Safety I: block a user**, the first ⬜ PENDING Day (Days 1–4 ✅). The multi-night frozen-main escalation is **resolved**: `main` is now 0 behind the latest nightly (the founder merged PR #144 and installed the auto-ff action), so the escalation guard did not trip and plan execution resumed. Shipped a `UserBlock` model + block/unblock route with **mutual** enforcement across all four presence-leaking surfaces, block UI, and 20 new tests. All Day 5 acceptance criteria met → Day 5 marked ✅ COMPLETE. PR [#145](https://github.com/pcettina/OutTheGroupchat/pull/145).

## Build-Plan Status
- **Task source:** PLAN
- **Active Day:** Day 5 — `nightly/2026-07-04` (label) — Trust & Safety I: block a user — executed on catch-up branch `nightly/2026-07-16`
- **Day goal:** Blocking a user hides them from your Crew/feed/heatmap/check-in surfaces (and vice-versa).
- **Progress tonight:** 5/5 Day tasks landed green (L1, M1×4 surfaces, M2, T1a, T1b)
- **Day status after tonight:** ✅ COMPLETE
- **Days remaining in plan:** 9 (Days 6–14)
- **Reference:** `docs/BUILD_PLAN.md` §2

## Pipeline Execution
| Phase | Status | Notes |
|-------|--------|-------|
| Build-Plan Alignment (P0) | ✅ | Source: PLAN Day 5 |
| Git Sync | ✅ | Escalation cleared — main 0 behind latest nightly; based on `origin/main` |
| Prev Recommendations | ✅ | Source: memory/local — "merge PR #144 to unblock → run Day 5" (both satisfied) |
| Codebase Analysis | ✅ | Consolidated (health + security snapshot agent) |
| Automated Small Tasks | ✅ | Metrics via analysis agent |
| Task Generation | ✅ | From BUILD_PLAN Day 5 verbatim |
| Wave 1 (Tests) | ✅ | 2 agents — 20 tests |
| Wave 2 (Features) | ✅ | 6 agents (L1 foundation → 5 parallel surfaces/mock/UI) |
| Wave 3 (Shared Files) | ✅ | 1 agent (docs) + setup.ts (Stage B) |
| Validation | ⚠️ | tsc/lint/test/prisma green; `next build` worker env-crash (see below) |
| Git & PR | ✅ | PR #145 |
| Notion Report | ⚠️ | Notion MCP unauthenticated in headless run → local fallback (this file) |

## Wave 1: Test Writers
1. [T1a] `user-block.test.ts` — success — 12 tests (block/unblock happy-path, lexicographic pair sort, double-block idempotent, unblock idempotent, 401 unauth, 400 self-block, 404 not-found, 429 rate-limited, crew-edge sever asserted)
2. [T1b] `block-enforcement.test.ts` — success — 8 tests (one enforcement + one control per surface: crew, feed, heatmap, checkins)

## Wave 2: Features & Refactors
1. [L1] UserBlock model + `POST/DELETE /api/users/[userId]/block` — success — additive idempotent migration; auto-severs Crew edge via `prisma.$transaction([userBlock.upsert, crew.deleteMany])`; Zod+auth+Sentry+pino
2. [M1a] Block enforcement — crew list (`api/crew/route.ts`) — success
3. [M1b] Block enforcement — feed (`api/feed/route.ts`) — success (meetup.hostId + checkIn.userId `notIn`)
4. [M1c] Block enforcement — heatmap aggregation (`lib/heatmap/aggregate.ts`) — success (threaded `excludeUserIds` through `fetchContributions`; no signature break)
5. [M1d] Block enforcement — check-in feed (`api/checkins/feed/route.ts`) — success
6. [M2] Block UI — success — new `components/safety/BlockButton.tsx` wired into profile + crew list (confirm step; warns that blocking severs the Crew edge)

Enforcement is **mutual and symmetric** (hides anyone the viewer blocked OR who blocked the viewer) — no partial leak on any of the four surfaces.

## Wave 3: Shared File Updates
- `setup.ts`: `userBlock` mock (upsert/deleteMany/findMany/findUnique/count/create/delete) + `$transaction` now resolves array form
- `BUILD_PLAN.md`: Day 5 → ✅ COMPLETE (PR #145, 2026-07-16)
- `API_STATUS.md`, `CURRENT_SPRINT.md`, `CODEMAP.md`, `LAUNCH_CHECKLIST.md`: updated + dated

## In-run regressions (introduced, then fixed — final suite green)
- The four enforcement surfaces call `prisma.userBlock.findMany` unconditionally; pre-existing tests using `vi.resetAllMocks` left it returning `undefined` → `flatMap` threw → 74 failures. **Fix:** coalesced to `[]` (`?? []`) on all four surfaces (prod-safe; real Prisma returns `[]`).
- The two feed tests declare their **own local** `vi.mock('@/lib/prisma')` lacking `userBlock` → `prisma.userBlock` undefined → 500. **Fix:** added a `userBlock` override (`findMany → []`) to both local mocks.
- Result: 1929/1929 passing.

## Tasks Failed
None.

## Codebase Health Metrics
| Metric | Previous | Current | Trend |
|--------|----------|---------|-------|
| Tests | 1909 | 1929 | ↑ (+20) |
| any types | 0 | 0 | → |
| console.* | 0 | 0 | → |
| TODO/FIXME | 0 | 0 | → |
| Files >600 lines | 0 | 0 | → |
| API routes (live, excl _archive) | 63 | 64 | ↑ (+1 block route) |
| Test files (`*.test.ts`) | 119 | 121 | ↑ (+2) |

## Build & Test Results
- **Build:** ⚠️ `next build` **compiles successfully** ("✓ Compiled successfully") but the Next 14.2.35 type-check/lint worker crashes on this Windows host with exit `3221225794` (`0xC0000374`, heap corruption), alongside a pre-existing "Found lockfile missing swc dependencies, patching… Failed to patch lockfile" quirk. Reproduced identically twice. This is an **environment-level worker flake, not a code defect** — the actual checks it runs are independently green.
- **tsc (standalone, full tree):** 0 errors
- **Lint:** 0 warnings, 0 errors
- **Tests:** 1929 passed, 0 failed (99 files)
- **Prisma:** valid

## Security Findings
No new gaps. The block route enforces getServerSession + Zod + self-block guard + rate-limit + Sentry. Block enforcement is mutual across all four presence-exposing surfaces (no partial leak). The 14 routes without a session check are all by-design public/cron/auth endpoints (unchanged from baseline). `/api/search` continues to omit user email.

## Next Day Recommendations
1. **[HIGH] Investigate the `next build` type-check worker crash (`0xC0000374`) on the Windows host.** Compile succeeds and tsc/lint/tests/prisma are all green, so this is environmental. Try `npm ci` (or delete `node_modules` + reinstall `next`) to resolve the "lockfile missing swc dependencies / failed to patch lockfile" state, and confirm the **Linux CI build passes** for PR #145 (CI is the source of truth for merge-gating, not this local worker).
2. **[HIGH] Merge PR #145** to advance the plan; the auto-ff action should keep `main` current. Next nightly runs **Day 6 — Trust & Safety II: report + anti-spam** (depends on Day 5 ✅, now satisfied).
3. **[MEDIUM] Add a GET block-state endpoint** (or fold block status into the profile/crew payloads). The M2 UI currently derives block state best-effort (session-local on the crew list); a canonical read would make the toggle authoritative on reload.
4. **[MEDIUM] Consider a shared `getBlockedUserIds(prisma, userId)` helper** in `src/lib/` reused by all four surfaces + the heatmap (the aggregate already has a local version). Reduces the four near-duplicate block-fetch blocks to one and standardizes the `?? []` guard.
5. **[LOW] Reconcile CODEMAP route/TS counts** — prior docs under-counted (62 routes / 416 TS); this run used reproducible `find` results (64 routes incl. new / 424 TS-TSX). Worth a one-time audit so the codemap drift stops.

These will be picked up by the next nightly build.

## Errors & Warnings
- Notion MCP unauthenticated in this headless run → report saved locally (this file) per the fallback chain.
- `next build` worker heap-corruption crash (documented above) — non-blocking for code correctness; flagged as rec #1.
- Bash cwd-doubling hit once (`app/app`) then corrected with absolute paths (known gotcha).

---
*Generated by OutTheGroupchat Nightly Build Coordinator v2 — plan-driven (BUILD_PLAN.md Day 5)*
