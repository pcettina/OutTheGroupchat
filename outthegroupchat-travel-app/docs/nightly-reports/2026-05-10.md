# Nightly Build Report — 2026-05-10

## Summary
Surgical-mode nightly: re-fixed the recurring TSC regression in `prisma/scripts/seed-heatmap-only.ts` (carries back from main because PR #102/#103 are still unmerged), added repo-root `.gitattributes` to stop the recurring LF→CRLF warning chain on Windows checkouts, and instrumented two auxiliary live routes (`/api/health`, `/api/geocoding`) with Sentry — non-conflicting with PR #103's V1 hot-path additions, so both can land independently. All 1081 tests, lint, TSC, prisma, and the production build are green.

## Refactor Phase Status
- **Active phase:** Phase 8 — launch-readiness re-audit
- **Objective:** Re-validate launch checklist + Sentry coverage + observability gates against the V1 vision
- **Progress tonight:** Sentry coverage 39/59 → 41/59 on this branch (69.5%); recurring Windows line-ending paper-cut closed at the `.gitattributes` layer
- **Phase exit:** Not yet — gated on (a) PR backlog landing/closing (11+ unmerged nightlies), (b) Sentry DSN set in Vercel production env, (c) E2E Playwright authenticated flow for the V1 loop
- **Reference:** `outthegroupchat-travel-app/docs/REFACTOR_PLAN.md` §5 Phase 8

## Pipeline Execution
| Phase | Status | Notes |
|-------|--------|-------|
| Refactor Alignment (P0) | ✅ | Active: Phase 8 |
| Git Sync (P1) | ✅ | `nightly/2026-05-10` from main (`fd3e5ae`) |
| Prev Recommendations (P2) | ✅ | Source: local fallback `2026-05-09.md` (Notion not invoked) |
| Codebase Analysis (P3) | ✅ | Direct shell analysis (surgical mode) |
| Automated Small Tasks (P3.5) | ✅ | Metrics computed |
| Task Generation (P4) | ✅ | 4 surgical tasks (TSC fix, .gitattributes, Sentry ×2, CODEMAP) |
| Wave 1 (Tests) | — | Skipped (surgical mode — no new test files needed) |
| Wave 2 (Features) | ✅ | Inline edits (TSC, gitattributes, Sentry) |
| Wave 3 (Shared Files) | ✅ | CODEMAP.md header refreshed |
| Validation (P6) | ✅ | TSC 0, lint 0, 1081/1081 tests, prisma valid, build PASS |
| Git & PR (P7) | ✅ | PR #104 — https://github.com/pcettina/OutTheGroupchat/pull/104 |
| Notion Report (P8) | ⚠️ | Local fallback (this file) — Notion mcp not invoked tonight |

## Tasks Completed

1. **TSC fix — `prisma/scripts/seed-heatmap-only.ts`**: dropped `.ts` extensions on relative imports of `../seed/generators/heatmap-fixtures` and `../seed/generators/topics`. Same fix as PR #102 and PR #103; carries back to every `nightly/*` branched off `main` until one of those PRs merges.
2. **`.gitattributes` (new file at repo root)**: `* text=auto eol=lf` plus an explicit binary list (images, fonts, PDFs, archives). Resolves yesterday's LOW recommendation #6 — every nightly that touched a doc file has been emitting `LF will be replaced by CRLF` warnings on Windows checkouts. This is a one-shot fix and won't recur.
3. **Sentry instrumentation — 2 auxiliary live routes:**
   - `src/app/api/health/route.ts` — `captureException` on the `prisma.$queryRaw` failure path (the previous `catch {}` swallowed the error). Tags: `route=/api/health, op=db_ping`.
   - `src/app/api/geocoding/route.ts` — `captureException` on the Nominatim fetch failure path. Tags: `route=/api/geocoding, query`.
   - These are deliberately routes NOT touched by PR #103 (which added V1 hot paths: profile, search, users/me, users/[userId], pusher/auth). Picking non-overlapping routes keeps both PRs landable independently.
4. **CODEMAP.md header refresh** — branch stats updated: 59 routes / 64 test files / 1081 tests / 291 TS-TSX / Sentry 41/59 (69.5%) on this branch (44/59 once #103 lands).

## Codebase Health Metrics

| Metric | Previous (2026-05-09 branch) | Current (2026-05-10 branch) | Trend |
|--------|------------------------------|------------------------------|-------|
| Tests | 1081 | 1081 | → |
| `any` types (prod) | 0 | 0 | → |
| `console.*` (prod) | 0 | 0 | → |
| TODO/FIXME | 0 | 0 | → |
| Files >600 lines (prod) | 2 | 2 | → |
| API routes (live) | 59 | 59 | → |
| API routes (with Sentry, this branch) | 39 | 41 | ↑ |
| Sentry live-route coverage (this branch) | 66.1% | 69.5% | ↑ |
| Test files | 64 | 64 | → |

> Note on the previous-night column: PR #103 brought the `nightly/2026-05-09` branch to 44/59 (74.6%) but is unmerged, so the value of Sentry-instrumented routes *on `main`* is 39 — that is the baseline tonight's branch was cut from. Once #103 merges, this branch jumps to 46/59 (78.0%) without further work.

## Build & Test Results

- **Build:** PASS
- **Lint:** 0 warnings, 0 errors
- **Tests:** 1081 passed / 1081 total (64 files)
- **Prisma:** valid
- **TSC:** 0 errors

## Security Findings

No new findings. AI surface remains fully removed (PR #65). Production env blockers unchanged: Sentry DSN missing in Vercel, Pusher env vars missing, Resend domain not verified, DEMO_MODE=false.

## Tasks Failed

None.

## Next Day Recommendations

1. **[CRITICAL]** Land or close the nightly PR backlog. As of tonight there are **11 open nightly PRs** (#94, #96–#103) plus #95 (auth aesthetic). The TSC regression that re-appears every single night, the missing `SENTRY_COVERAGE_AUDIT.md` on main, and the V1 hot-path Sentry coverage that vanishes between branches all trace to the same root cause: nothing is merging. Suggested triage path:
   - Squash-merge #94 (TSC + CODEMAP) and #96 (TSC + planning rollup) directly to main — both are pure non-conflicting fixes.
   - Close #97–#101 as superseded by #103 if their substantive changes are subsumed; otherwise rebase forward.
   - Land #103 (Sentry V1 hot paths + LAUNCH_CHECKLIST rewrite) as the canonical state-of-launch doc.
   - Once #103 lands, tonight's PR rebases trivially and adds geocoding/health on top.
2. **[HIGH]** Set `SENTRY_DSN` in Vercel production env. Every `captureException` call added across the last 6 nightlies is currently no-op'ing in production. Single highest-leverage env action remaining for Phase 8 exit.
3. **[HIGH]** E2E Playwright authenticated flow for the canonical V1 loop — signup → set Intent → match into Subcrew → create Meetup → check in. `e2e/auth-flow.spec.ts` and `e2e/crew.spec.ts` exist for public flows; the authenticated end-to-end loop is the gap.
4. **[MEDIUM]** Decide deprecation/archive on the 8 Priority-3 trip-era routes from `docs/SENTRY_COVERAGE_AUDIT.md` (once that doc lands via PR #103): `discover/import`, `discover/recommendations`, `discover/search`, `inspiration`, `invitations/*`, `images/search`, `newsletter/subscribe`. Archive (move under `_archive/`) is preferred over instrumentation since these don't serve V1.
5. **[MEDIUM]** Set Pusher env vars (6) in Vercel production. Real-time meetup updates and check-ins silently degrade without these.
6. **[LOW]** Sentry on the remaining auxiliary routes not in any unmerged PR: `cron`, `beta/initialize-password`, `beta/signup`, `beta/status`. Each is a 4-line edit.
7. **[LOW]** Rebase or close PR #95 (`feat/auth-v1-aesthetic-and-redirect`) — it's been open ~13 days and is now the oldest non-nightly PR.

## Errors & Warnings

- Same TSC regression in `seed-heatmap-only.ts` re-applied for the 3rd nightly in a row. Will recur until PR #102 or #103 merges.
- No LF→CRLF warnings expected on tonight's commit thanks to the new `.gitattributes` (one-shot benefit on subsequent edits — the initial creation will still touch CRLF→LF normalization on already-checked-out files).

---
*Generated by OutTheGroupchat Nightly Build Coordinator — surgical-fix mode*
