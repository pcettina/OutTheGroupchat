# Nightly Build Report — 2026-07-21

**Branch:** `nightly/2026-07-22` · **PR:** https://github.com/pcettina/OutTheGroupchat/pull/150 · **Status:** ✅ Success

## Summary

Tenth feature build of the plan-driven nightly arc — BUILD_PLAN.md §2 **Day 10 "Growth: FoF Crew suggestions + ping nearby"**, run on catch-up branch `nightly/2026-07-22` for the Day-10 label `nightly/2026-07-09`. Reused the friend-of-friend graph for "People you may know" suggestions and added one-tap pinging of nearby checked-in Crew. All 3 tasks (M1, M2, T1) landed green plus a carried security fix (auth-guarding the previously-unauthenticated `GET /api/feed/comments`), with **no `schema.prisma` or `setup.ts` changes needed** (the `CREW_CHECKED_IN_NEARBY` enum and all Prisma mocks were already present).

## Build-Plan Status

- **Task source:** PLAN (`docs/BUILD_PLAN.md`)
- **Active Day:** Day 10 — Growth: FoF Crew suggestions + ping nearby (run on catch-up branch `nightly/2026-07-22`)
- **Day goal:** "People you may know" from the FoF graph; one-tap ping to nearby active Crew.
- **Progress:** 3/3 tasks complete, 0 carried
- **Day status:** ✅ COMPLETE
- **Days remaining:** 4 (Days 11–14)
- **Reference:** BUILD_PLAN.md §2

## Pipeline Execution

| Phase | Step | Status |
|-------|------|--------|
| 1 | Git sync + intelligent merge | ✅ main 0 behind — no escalation (auto-ff working) |
| 2 | Previous-day recommendation review | ✅ |
| 3 | Codebase analysis + Day 10 task generation | ✅ |
| 4 | Wave 1 — tests | ✅ (ran after Wave 2 features) |
| 5 | Wave 2 — features | ✅ (ran before Wave 1) |
| 6 | Validation (tsc/lint/vitest/prisma) | ✅ |
| 7 | Wave 3 — shared-file consolidation | ✅ |
| 8 | PR creation + git push + Notion log | ✅ |

> Note: Wave 2 (features) ran before Wave 1 (tests) so the tests assert landed code. Git sync found `main` 0 nightlies behind the clean linear tip — no escalation guard tripped; the auto-ff-main action is confirmed working.

## Wave 2 — Features

- **[M1] Crew suggestions** — NEW `src/app/api/crew/suggestions/route.ts` (GET) reusing `src/lib/heatmap/fof-graph.ts`; ranks FoF by mutual count and excludes existing/PENDING Crew + blocked users (the block filter degrades gracefully if `UserBlock` is absent, honoring Day 10's Day-5 dependency note). Guard chain auth → rate-limit → Zod → Sentry. NEW `src/components/crew/SuggestionCard.tsx` + a suggestions block on `src/app/crew/page.tsx` with inline Add (POST `/api/crew/request`).
- **[M2] Ping nearby Crew** — NEW `src/app/api/checkins/ping/route.ts` (POST) pings active accepted-Crew check-ins near the caller, creating `CREW_CHECKED_IN_NEARBY` notifications; rate-limited via the existing `checkRateLimit` (`src/lib/rate-limit.ts`). MODIFIED `src/components/checkins/NearbyCrewList.tsx` — added Ping buttons + a defensive feed-shape read (`/api/checkins/feed` returns a bare array while the component expected `{items}`).
- **[M3] Carried security fix** — MODIFIED `src/app/api/feed/comments/route.ts`: the GET handler previously had **no auth check**, allowing unauthenticated comment enumeration. Added a `getServerSession()` 401 guard.

## Wave 1 — Test Writers

- **[T1] NEW `src/__tests__/api/crew-suggestions.test.ts` (14)** — FoF ranking by mutual count, existing/PENDING-Crew + blocked-user exclusion, graceful degradation without `UserBlock`, auth/rate-limit/Zod paths.
- **[T1] NEW `src/__tests__/api/checkins-ping.test.ts` (15)** — nearby active-Crew targeting, `CREW_CHECKED_IN_NEARBY` dispatch, rate-limit 429, auth paths.
- **[T1] MODIFIED `src/__tests__/api/feed-comments-engagement.test.ts` (+1 GET-401 regression)** — the 6 pre-existing GET tests were updated to supply a session after the GET auth guard landed.

## Wave 3 — Shared File Updates

- `docs/BUILD_PLAN.md` — Day 10 marked ✅ COMPLETE (PR #150, 2026-07-21); carry-over line notes the M3 security fix + the `/api/checkins/feed` shape-normalization follow-up.
- `docs/API_STATUS.md` — added `GET /api/crew/suggestions` + `POST /api/checkins/ping`; noted `GET /api/feed/comments` is now auth-guarded; Last Updated → 2026-07-21; route count → 68 live.
- `docs/CURRENT_SPRINT.md` — added "Completed 2026-07-21 (Day 10)" section; test metric → 2173.
- `docs/CODEMAP.md` — Last updated → 2026-07-21; stats → 2173 tests / 110 files / 68 live routes; new route + test + component rows added; Codebase Health block updated.
- `docs/LAUNCH_CHECKLIST.md` — checked off FoF suggestions, ping nearby, and the feed-comments auth-guard security item; Last Updated → 2026-07-21; test count → 2173.
- `src/__tests__/setup.ts` — **no-op** (no new mocks needed; all delegates already present).

## Tasks Failed

None.

## Codebase Health Metrics

| Metric | Previous | Current | Trend |
|--------|----------|---------|-------|
| Tests | 2143 | 2173 | ↑ |
| Test files | 108 | 110 | ↑ |
| `any` types | 0 | 0 | → |
| `console.*` | 0 | 0 | → |
| TODO/FIXME | 0 | 0 | → |
| prod files >600 lines | 0 | 0 | → |
| Live API routes | 66 | 68 | ↑ |

## Build & Test Results

- **tsc:** 0 errors
- **lint:** 0 warnings / 0 errors
- **vitest:** 2173 passed / 0 failed / 110 test files
- **prisma:** valid
- **build:** skipped — the Next 14 type-check worker crashes on this Windows host (exit 0xC0000374, heap corruption), a known environment flake; gating is on standalone tsc + lint + vitest + prisma.

## Security Findings

- **FIXED (M3):** `GET /api/feed/comments` is now guarded with `getServerSession()` (was unauthenticated → comment enumeration).
- **Observed / follow-up:** `GET /api/checkins/feed` returns a bare array while consumers expect `{ items }` — a shape drift patched defensively in `NearbyCrewList` this run; server-side normalization is recommended (see rec #2).
- Zod input-validation coverage remains 100% on all API routes; `/api/search` still omits `email` from its projection; `/api/crew/[id]` does not return member `email` in its responses (a prior concern — resolved / mis-scoped).

## Next Day Recommendations

1. **[HIGH]** Apply the additive `onboardedAt` column to Neon prod (carried since Day 7 — `/api/users/onboarding` errors in prod until the column exists).
2. **[MEDIUM]** Normalize `GET /api/checkins/feed` to return `{ items: [...] }` (or standardize all consumers) — the array-vs-`items` drift currently makes `NearbyCrewList` render empty except for the defensive patch.
3. **[MEDIUM]** Add `?topicId` prefill to `IntentCreateForm` so Day 9's `buildTopicIntentHref()` deep-links work (one-line + one test).
4. **[MEDIUM]** Promote the inline `getBlockedUserIds` idiom (duplicated across crew/feed/heatmap/checkins/suggestions) into one shared exported helper.
5. **[LOW]** Set `ADMIN_USER_IDS` in Vercel prod (carried).
6. **[LOW]** Prune dead code (`src/components/feed/FeedItemTypes.ts` ~12 unused exports; ~14 total outside `_archive`).

Next scheduled work: **Day 11 — Meetup depth: edit/cancel + .ics + @mentions.**

## Errors & Warnings

None blocking.

---
*Generated by OutTheGroupchat Nightly Build Coordinator v2 — plan-driven (BUILD_PLAN.md)*
