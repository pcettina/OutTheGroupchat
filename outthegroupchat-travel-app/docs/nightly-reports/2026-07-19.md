# Nightly Build Report — 2026-07-19

## Summary
Executed **BUILD_PLAN.md Day 7 — Activation: real onboarding + empty/error states** (labeled `nightly/2026-07-06`) on catch-up branch `nightly/2026-07-19`. Shipped a real 3-step onboarding flow (Topics → find/add Crew → first Intent) on a live `/onboarding` route, gated by a new `User.onboardedAt` completion field + post-signup redirect, and closed the "blank/silent-fail surface" gaps with a shared `ErrorBanner` + first-run `EmptyState`s across the intents/subcrews/feed pages. All 3 Day-7 tasks landed green → Day 7 marked ✅ COMPLETE. Main was 0 nightlies behind at start (auto-ff working), so the escalation guard was clear and PLAN execution proceeded normally. PR [#147](https://github.com/pcettina/OutTheGroupchat/pull/147).

## Build-Plan Status
- **Task source:** PLAN
- **Active Day:** Day 7 — Activation: real onboarding + empty/error states
- **Day goal:** First-run flow (add Crew → post first Intent) on a real route; every core surface has an empty state + visible error banner.
- **Progress tonight:** 3/3 Day tasks complete (L1 data + L1 UI + M1 + T1)
- **Day status after tonight:** ✅ COMPLETE (PR #147)
- **Days remaining in plan:** 7 (Days 8–14 ⬜ PENDING)
- **Reference:** `outthegroupchat-travel-app/docs/BUILD_PLAN.md` §2

## Pipeline Execution
| Phase | Status | Notes |
|-------|--------|-------|
| Build-Plan Alignment (P0) | ✅ | Source: PLAN Day 7 (first non-✅ Day; Days 1–6 ✅) |
| Git Sync | ✅ | main == latest nightly (0 behind, auto-ff working); base on main |
| Prev Recommendations | ✅ | Source: local report 2026-07-17 |
| Codebase Analysis | ✅ | Day-7 scout + health/security scan (both green baseline) |
| Automated Small Tasks | ✅ | metrics captured |
| Task Generation | ✅ | Day 7 tasks verbatim (A/L1, B/L1, C/M1, T1) |
| Wave 2 (Features) | ✅ | 3 parallel agents, file-partitioned (A/B/C) |
| Wave 1 (Tests) | ✅ | co-shipped with features (greenfield) — +30 tests |
| Wave 3 (Shared Files) | ✅ | 1 agent (5 docs + Day-7 mark); setup.ts unchanged |
| Validation | ✅ | tsc 0 / lint 0-0 / vitest 1976 / prisma valid |
| Git & PR | ✅ | PR #147 |
| Notion Report | ✅ | Created in Nightly Build Reports DB + local copy (this file) |

> Note on wave order: Day 7's onboarding route/UI were **greenfield** (empty `src/app/onboarding/`, dead `InterestSelector`), so a separate Wave-1 test pass couldn't read not-yet-existing code. Each Wave-2 agent co-shipped and self-verified its own tests instead. Reasonable-choice deviation, noted per autonomous-run guidance.

## Wave 2: Features & Refactors
1. **[A/L1] Onboarding data layer** — success. Additive `User.onboardedAt DateTime?`; NEW `POST/GET /api/users/onboarding` (`src/app/api/users/onboarding/route.ts`) — GET returns `{onboarded, onboardedAt}`, POST stamps `onboardedAt=now` (idempotent overwrite); guard chain auth → rate-limit → Zod → Sentry → pino. MODIFIED `src/app/auth/signup/page.tsx`: brand-new signups redirect to `/onboarding` (`POST_SIGNUP_ONBOARDING_PATH`; explicit `callbackUrl` still honored). `prisma generate` run.
2. **[B/L1] Onboarding UI flow** — success. NEW `/onboarding` 3-step client flow (`src/app/onboarding/page.tsx`): Topics → Crew → first Intent. REWROTE dead trip-era `InterestSelector.tsx` into a real Topic selector (fetches `GET /api/topics`, `{id,displayName}` chips, `otg-*` tokens; stale hardcoded travel array dropped). NEW `OnboardingCrewStep.tsx` (`/crew` deep link + skip), `OnboardingIntentStep.tsx` (reuses `IntentCreateForm`), `onboardingFlow.ts` (framework-free gating/completion helpers). Marks `POST /api/users/onboarding` then lands on `/intents`; self-skips via GET if already onboarded.
3. **[C/M1] Empty + error states** — success. NEW shared `src/components/ui/ErrorBanner.tsx` (role=alert, Retry/Dismiss, `AlertCircle`), exported from `ui/index.ts`. Feed's two silent catches now surface a visible ErrorBanner with Retry; `intents`/`subcrews/[id]`/`feed` empty `<p>` blocks converted to shared `EmptyState` with CTAs; intents now surfaces endpoint-level `success:false` (not just network errors).

## Wave 1: Test Writers (+30)
1. [T1] `src/__tests__/api/onboarding.test.ts` — 13 tests (401 no-session, POST stamps onboardedAt, idempotency, topicIds not persisted, GET status).
2. [T1] `src/__tests__/onboarding-flow.test.ts` — 12 tests (topic-step gating, completion/skip calls POST + navigates to `/intents`, fail-graceful path, status/topic fetches, `/crew` deep link).
3. [T1] `src/__tests__/components/empty-error-states.test.ts` — 5 tests (ErrorBanner renders message/role=alert, Retry/Dismiss fire callbacks).

Tests follow the repo's node-env `renderToStaticMarkup` pattern (no jsdom/@testing-library; include glob matches `*.test.ts` only).

## Wave 3: Shared File Updates
- BUILD_PLAN.md: Day 7 → ✅ COMPLETE (PR #147, 2026-07-19)
- CURRENT_SPRINT.md / CODEMAP.md / API_STATUS.md / LAUNCH_CHECKLIST.md: updated, Last Updated 2026-07-19; `/api/users/onboarding` documented; stats reconciled
- setup.ts: **unchanged** — no new Prisma model; `onboardedAt` is a scalar on the existing `user` mock (`update`/`findUnique` already present)

## Tasks Failed
None.

## Codebase Health Metrics
| Metric | Previous | Current | Trend |
|--------|----------|---------|-------|
| Tests | 1946 | 1976 | ↑ +30 |
| Test files | 100 | 103 | ↑ +3 |
| any types | 0 | 0 | → |
| console.* | 0 | 0 | → |
| TODO/FIXME | 0 | 0 | → |
| Files >600 lines | 0 | 0 | → |
| API route.ts | 77 | 78 | ↑ +1 |

## Build & Test Results
- **Build:** `next build` gated out — known Next 14 type-check-worker heap crash (`0xC0000374`) on this Windows host; Linux CI is the merge gate.
- **tsc:** 0 errors
- **Lint:** 0 warnings, 0 errors
- **Tests:** 1976 passed / 1976 total
- **Prisma:** valid

## Security Findings
Baseline scan clean (no routes missing auth/Zod, no email exposure in search, no hardcoded secrets). The new `POST/GET /api/users/onboarding` follows the standard guard chain (auth → rate-limit → Zod → Sentry `captureException` → pino). No new surface leaks presence or PII.

## Next Day Recommendations
1. **[HIGH] Merge PR #147** to advance the plan (auto-ff keeps `main` current). Next nightly runs **Day 8 — Tighten the notification loop** (no hard deps).
2. **[HIGH] Apply the additive `onboardedAt` column to the Neon prod DB.** The schema change is validated + the client is generated, but this run did not push a prod migration (`prisma generate` only). Until the column exists in prod, `POST/GET /api/users/onboarding` will error server-side.
3. **[MEDIUM] Carry forward the two Day-5 follow-ups** (still open): a canonical GET block-state endpoint, and a shared `getBlockedUserIds(prisma, userId)` helper reused across the four block-enforcement surfaces + heatmap.
4. **[MEDIUM] Set `ADMIN_USER_IDS` in Vercel prod** (from Day 6) so the admin `GET /api/reports` list is reachable — still outstanding.
5. **[LOW] Investigate the Windows `next build` worker crash (`0xC0000374`)** so full-build validation can run locally again. Non-blocking — Linux CI remains the source of truth.

These recommendations will be picked up by tomorrow's nightly build.

## Errors & Warnings
- Notion MCP was authenticated this run — report page created in the Nightly Build Reports DB; local copy also kept.
- `next build` worker heap-corruption crash — non-blocking for code correctness; validation gated on tsc/lint/vitest/prisma.
- Greenfield onboarding meant tests co-shipped with features rather than a separate Wave 1 (documented above).

---
*Generated by OutTheGroupchat Nightly Build Coordinator v2 — plan-driven (BUILD_PLAN.md), refactor-heuristic fallback*
