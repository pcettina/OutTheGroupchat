# Nightly Build Report — 2026-07-22 (Day 11)

**Branch:** `nightly/2026-07-22` · **PR:** https://github.com/pcettina/OutTheGroupchat/pull/151 · **Status:** ✅ Success

> Note: this is the **Day 11** run. The Day 10 report is filed as `2026-07-22.md`; this run is filed as `2026-07-22-day11.md` to avoid clobbering it (Day 10 shipped on the same catch-up calendar date).

## Summary

Eleventh feature build of the plan-driven nightly arc — BUILD_PLAN.md §2 **Day 11 "Meetup depth: edit/cancel + .ics + @mentions"** (labeled `nightly/2026-07-10`, run on catch-up branch `nightly/2026-07-22`). Hosts can now edit or cancel a Meetup with attendees notified; anyone can add a Meetup to their calendar via a valid `.ics` export; and comments support `@mentions` that notify the mentioned user and render as styled links. All 3 feature tasks (M1, S1, S2) + 50 tests landed green with **no `schema.prisma` or `setup.ts` changes** (the `SYSTEM` + `data.kind` notification convention avoided an enum change, and every Prisma delegate was already mocked).

## Build-Plan Status

- **Task source:** PLAN (`docs/BUILD_PLAN.md`)
- **Active Day:** Day 11 — Meetup depth: edit/cancel + .ics + @mentions (run on catch-up branch `nightly/2026-07-22` for the Day-11 label `nightly/2026-07-10`)
- **Day goal:** Hosts edit/cancel (attendees notified); attendees add to calendar; comments support @mentions.
- **Progress tonight:** 3/3 tasks complete, 0 carried
- **Day status after tonight:** ✅ COMPLETE
- **Days remaining in plan:** 3 (Days 12–14)
- **Reference:** `docs/BUILD_PLAN.md` §2

## Pipeline Execution

| Phase | Status | Notes |
|-------|--------|-------|
| Build-Plan Alignment (P0) | ✅ | Source: PLAN Day 11 |
| Git Sync (P1) | ✅ | main 2 behind branch (< 5) — no escalation; auto-ff merged PR #150 (Day 10) to main |
| Prev Recommendations (P2) | ✅ | Source: local report `2026-07-22.md` (Day 10); Notion unavailable (non-interactive OAuth) |
| Codebase Analysis (P3) | ✅ | 3 background agents (security/quality-build/test-coverage) + 1 Day-11 recon agent |
| Automated Small Tasks (P3.5) | ✅ | metrics captured |
| Task Generation (P4) | ✅ | Day 11 tasks verbatim (M1/S1/S2 + T1) |
| Wave 2 (Features) | ✅ | 3 agents, ran before Wave 1 so tests assert landed code |
| Wave 1 (Tests) | ✅ | 3 agents, +50 tests |
| Wave 3 (Shared Files) | ✅ | 1 agent; caught a Wave-1 tsc regression (fixed by coordinator) |
| Validation (P6) | ✅ | tsc 0 · lint 0/0 · vitest 2223/2223 · prisma valid |
| Git & PR (P7) | ✅ | PR #151 |
| Report (P8) | ⚠️ | Notion unavailable → local report only |

## Wave 2: Features

1. **[M1] Meetup edit/cancel UI + attendee notifications** — ✅
   - `src/app/api/meetups/[id]/route.ts`: added fail-soft `notification.createMany` to the existing PATCH + DELETE handlers (`type: SYSTEM`, `data.kind: MEETUP_UPDATED` / `MEETUP_CANCELLED`, host excluded). No enum/schema change — matches the `per-member-intent` convention. Existing Pusher broadcasts preserved.
   - New `src/components/meetups/EditMeetupModal.tsx` (host-only, reuses the create-flow field components + inline error banner + ESC-to-close).
   - `src/app/meetups/[id]/page.tsx`: host-only Edit button opens the modal (refetch on update); kept Cancel; added "Add to calendar" download anchor.
2. **[S1] .ics export** — ✅ New `src/app/api/meetups/[id]/ics/route.ts`: hand-rolled RFC 5545 VCALENDAR (UTC `DTSTART`/`DTEND`, `endsAt` fallback +2h, TEXT escaping + 75-octet line folding, `GEO` when lat/lng present, `STATUS:CANCELLED`). Mirrors GET's auth + visibility gate + `meetup-ics:<id>` rate-limit; returns `text/calendar` attachment.
3. **[S2] @mentions** — ✅ New `src/lib/mentions.ts` (pure `extractMentionTokens` / `tokenizeWithMentions`); `src/app/api/feed/comments/route.ts` resolves `@handle` against `User.name` (case-insensitive, incl. whitespace-stripped) and fires fail-soft MENTION notifications (author + already-notified owner excluded); `src/components/feed/CommentThread.tsx` renders mention runs as styled spans.

## Wave 1: Test Writers (+50)

1. `src/__tests__/lib/mentions.test.ts` — 18 tests (extract/tokenize edge cases)
2. `src/__tests__/api/feed-comments-mentions.test.ts` — 5 tests (resolve/notify, self-exclusion, owner de-dup, fail-soft)
3. `src/__tests__/api/meetup-ics.test.ts` — 19 tests (VCALENDAR shape, headers, cancelled, DTEND fallback, GEO, escaping, auth/visibility/rate-limit)
4. `src/__tests__/api/meetup-notify.test.ts` — 8 tests (PATCH/DELETE notify shape, host exclusion, 403 no-notify, fail-soft, Pusher preserved)

## Wave 3: Shared File Updates

- `docs/BUILD_PLAN.md`: Day 11 → ✅ COMPLETE (PR #151, 2026-07-22)
- `docs/API_STATUS.md`: new `.ics` route + PATCH/DELETE notify + comment @mention annotations; live routes 68 → 69; date 2026-07-22
- `docs/CURRENT_SPRINT.md`: Completed 2026-07-22 (Day 11) section; tests → 2223
- `docs/CODEMAP.md`: 2223 tests / 114 files / 69 routes; health block
- `docs/LAUNCH_CHECKLIST.md`: meetup edit/cancel, .ics, @mentions checked; date 2026-07-22
- `src/__tests__/setup.ts`: **no-op** (no new mocks needed — all delegates present)

## Tasks Failed

None. One in-flight issue caught + fixed: a Wave-1 assertion cast in `feed-comments-mentions.test.ts:137` was **tsc-red** (TS2352) though vitest (esbuild, no full typecheck) passed it. Wave 3's tsc gate surfaced it; the coordinator fixed the cast via `as unknown as` (compile-time only — the 5 tests still pass) restoring tsc 0. **Process note:** re-run `tsc --noEmit` after Wave 1, not just vitest — vitest does not full-typecheck.

## Codebase Health Metrics

| Metric | Previous (Day 10) | Current | Trend |
|--------|-------------------|---------|-------|
| Tests | 2173 | 2223 | ↑ +50 |
| any types | 0 | 0 | → |
| console.* | 0 | 0 | → |
| TODO/FIXME | 0 | 0 | → |
| prod files >600 lines | 0 | 0 | → |
| Live API routes | 68 | 69 | ↑ +1 (.ics) |
| Live test files | 110 | 114 | ↑ +4 |

## Build & Test Results

- **Build:** not gated (`npm run build` Windows type-check worker has a known heap-corruption env flake) — gated on standalone tsc/lint/vitest/prisma instead
- **tsc:** 0 errors
- **Lint:** 0 warnings, 0 errors
- **Tests:** 2223 passed / 2223 (114 files)
- **Prisma:** valid
- **Diff:** 21 files changed, +1873 / −40 (includes 2 carried topics hotfix commits)

## Security Findings

Security scan clean. Notable resolutions confirmed this run:
- `GET /api/feed/comments` now HAS a `getServerSession` guard (the previously-flagged unauth comment enumeration is RESOLVED, Day 10).
- `/api/search` omits `email` from its projection; `/api/crew/[id]` (PATCH/DELETE only) never returns member `email` in a response body — the prior email-leak notes are stale.
- All cron routes are `CRON_SECRET`-guarded (fail-closed); every route reading `req.json()` runs Zod.

## Next Day Recommendations

1. **[HIGH]** Apply the additive `onboardedAt` column to Neon prod (carried since Day 7 — `/api/users/onboarding` errors in prod until the column exists).
2. **[MEDIUM]** Add a unique `username`/`handle` column to `User` + backfill so @mentions resolve deterministically — the Day 11 mention feature currently matches non-unique `User.name`, so an ambiguous display name notifies every match and nameless users can't be mentioned.
3. **[MEDIUM]** Add a lightweight test for `src/app/api/crew/suggestions/route.ts` (Day 10 FoF suggestions still has no dedicated test file).
4. **[MEDIUM]** Normalize `GET /api/checkins/feed` to `{ items: [...] }` (carried from Day 10 — array-vs-`items` drift; only defensively patched in `NearbyCrewList`).
5. **[MEDIUM]** Add `?topicId` prefill to `IntentCreateForm` so Day 9's `buildTopicIntentHref()` deep-links work (carried; one-line + one test).
6. **[LOW]** Promote the duplicated inline `getBlockedUserIds` idiom into one shared exported helper (carried).
7. **[LOW]** Set `ADMIN_USER_IDS` in Vercel prod (carried); prune dead `FeedItemTypes.ts` exports (carried).

Next scheduled work: **Day 12 — Profile + auth depth (avatars, session timeout, failed-login backoff).**

## Errors & Warnings

- Notion MCP unavailable (non-interactive OAuth) → local report only; no Notion page created this run.
- `npm run build` deliberately not run (known Windows heap-corruption env flake); tsc/lint/vitest/prisma are the gate.
- Wave-1 tsc regression caught and fixed (see Tasks Failed). Lesson logged: run `tsc --noEmit` after Wave 1, not just vitest.

---
*Generated by OutTheGroupchat Nightly Build Coordinator v2 — plan-driven (BUILD_PLAN.md Day 11).*
