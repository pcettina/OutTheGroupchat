# Nightly Build Report — 2026-07-23

## Summary
Executed **BUILD_PLAN Day 12 — "Profile + auth depth"** on catch-up branch `nightly/2026-07-23` (the Day's target-branch label is `nightly/2026-07-11`; nights are running ahead of the calendar labels, which is expected). All three Day-12 feature tasks landed green: avatar upload that degrades gracefully to a 503 when no storage env is set, an explicit NextAuth session-timeout policy, and deterministic failed-login backoff. Two additional fixes shipped from tonight's analysis: a real security fix (unauthenticated user enumeration on `GET /api/users/[userId]`) and a mount fix so the new avatar UI is actually reachable. 149 net-new tests, all gates green, PR #152 opened against `main`.

## Build-Plan Status
- **Task source:** PLAN
- **Active Day:** Day 12 — "Profile + auth depth" (target label `nightly/2026-07-11`, run on catch-up branch `nightly/2026-07-23`)
- **Day goal:** Users upload an avatar (degrades gracefully without storage env); sessions have explicit timeout; failed logins back off.
- **Progress tonight:** 3/3 feature tasks + [T1] tests complete; +2 opportunistic fixes (security + mount)
- **Day status after tonight:** ✅ COMPLETE
- **Days remaining in plan:** 2 (Day 13 — Heatmap polish + GDPR deletion; Day 14 — PWA + share previews + safe schema hygiene)
- **Reference:** `outthegroupchat-travel-app/docs/BUILD_PLAN.md` §2

## Pipeline Execution
| Phase | Status | Notes |
|-------|--------|-------|
| Build-Plan Alignment (P0) | ✅ | Source: PLAN Day 12 |
| Git Sync | ✅ | main == nightly/2026-07-22 (0/0, auto-ff healthy); branched off main `2f241a4`; 0 open PRs; no escalation |
| Prev Recommendations | ✅ | Source: local `2026-07-22-day11.md` |
| Codebase Analysis | ✅ | 6 agents |
| Automated Small Tasks | ✅ | metrics collected |
| Task Generation | ✅ | Day 12 tasks verbatim; S1+M2 merged (same owner file); +S2 security, +S3 mount |
| Wave 1 (Tests) | ✅ | 3 agents, +149 tests |
| Wave 2 (Features) | ✅ | 3 agents (M1, S1+M2, S2) — ran BEFORE Wave 1 per standing lesson |
| Wave 3 (Shared Files) | ✅ | 1 agent |
| Validation | ✅ | tsc 0, lint 0/0, prisma valid, 2372/2372 |
| Git & PR | ✅ | PR #152 |
| Notion Report | ⚠️ | Notion unavailable (non-interactive OAuth); local report only |

## Wave 1: Test Writers (+149)
1. [T1] `src/__tests__/lib/storage.test.ts` — 47 tests (constants, `isStorageConfigured`, `sniffImageType` magic-byte authority, `putAvatar` via `deps.fetchImpl` seam, all `upload_failed` modes)
2. [T1] `src/__tests__/api/avatar.test.ts` — 25 tests (full POST status map incl. 503 `STORAGE_NOT_CONFIGURED`, 502, 413 boundary, magic-byte rejection; DELETE)
3. [T2] `src/__tests__/lib/login-backoff.test.ts` — 48 tests (lock re-stamp on hammer, full reset on served lockout, window decay, case/whitespace bypass resistance, reset-on-success, eviction)
4. [T2] `src/__tests__/lib/auth-session.test.ts` — 23 tests (maxAge/updateAge vs exported constants; `authorize()` lockout short-circuits before DB+bcrypt; no account-existence leak)
5. [T3] `src/__tests__/api/users.test.ts` — 15→21 (5 old-insecure-contract tests corrected + 6 invariant tests)

## Wave 2: Features & Security
1. [M1] Avatar upload — `src/lib/storage.ts` (dependency-free Vercel Blob wrapper; magic-byte sniff authoritative; `putAvatar` never throws) + `POST/DELETE /api/profile/avatar`; missing storage env → HTTP 503, not a crash; fixed `image` missing from `/api/profile` GET+PUT selects; tightened PUT Zod to match stricter sibling.
2. [S1] Session timeout — `src/lib/auth.ts`: `session.maxAge` 30d / `updateAge` 24h + aligned top-level `jwt.maxAge`, exported as named constants; threaded `image` through jwt/session callbacks.
3. [M2] Failed-login backoff — `src/lib/login-backoff.ts` (pure reducers + bounded in-memory store, injectable clock); wired into `authorize()` to short-circuit before DB lookup + bcrypt; not built on `checkRateLimit` (fails open without Upstash env → would have shipped inert).
4. [S2] Security fix — `GET /api/users/[userId]` called `getServerSession` but never returned 401, allowing unauthenticated enumeration; added auth gate + removed the client-writable `preferences` blob from the route (self-view still served by 401-gated `/api/users/me`).
5. [S3] Mount fix — created `/settings/profile` page + client wrapper; `ProfileSettings.tsx` was mounted by no page, so the avatar UI would have shipped inert.

## Wave 3: Shared File Updates
- BUILD_PLAN.md: Day 12 → ✅ COMPLETE (PR #152, 2026-07-23)
- API_STATUS.md: added `/api/profile/avatar`, corrected `GET /api/users/[userId]` auth status, 70 routes
- CURRENT_SPRINT.md: Day 12 completed section + metrics
- CODEMAP.md: stats, 4 new test files, `storage.ts` + `login-backoff.ts` in lib listing
- LAUNCH_CHECKLIST.md: checked off session policy / login limiting / enumeration fix; added `BLOB_READ_WRITE_TOKEN` prod blocker
- setup.ts: no changes needed (every agent reported `new_mocks_needed: []` — only `prisma.user` delegates used, already mocked)

## Tasks Failed
None.

## Codebase Health Metrics
| Metric | Previous | Current | Trend |
|--------|----------|---------|-------|
| Tests | 2223 | 2372 | ↑ +149 |
| Test files | 114 | 118 | ↑ +4 |
| API routes | 69 | 70 | ↑ +1 |
| any types | 0 | 0 | → |
| console.* | 0 | 0 | → |
| TODO/FIXME | 0 | 0 | → |
| Files >600 lines (prod) | 0 | 0 | → |

## Build & Test Results
- **Build gate:** tsc `--noEmit` 0 errors (used per standing Windows `next build` heap-corruption gotcha; standalone gates instead)
- **Lint:** 0 warnings, 0 errors
- **Tests:** 2372 passed / 2372 (118 files)
- **Prisma:** valid

## Security Findings (tonight's scan)
- **Fixed:** `GET /api/users/[userId]` unauthenticated enumeration + `preferences` leak (S2).
- **Open (HIGH):** No app-layer rate limit on the credentials login path itself beyond tonight's per-email backoff — `authRateLimiter` is wired into signup/reset/verify but not `[...nextauth]`. Tonight's `login-backoff` covers brute-force per-email; an IP-keyed layer is still a follow-up.
- **Open (HIGH):** `checkRateLimit` fails OPEN when Upstash env is unset (dev/prod current state) — every Upstash-backed limit is silently a no-op. Worth a production startup assertion.
- **Open (MEDIUM):** Password-reset / beta-init tokens stored in `VerificationToken` in plaintext (comment claims hashed; implementation disagrees). Store a SHA-256 digest.
- **Open (MEDIUM):** Email not normalized on the login lookup path (`authorize()` / signup / reset) while beta routes lowercase — case-variant duplicate accounts possible. Tonight's backoff key IS normalized, so the lock can't be case-bypassed.

## Next Day Recommendations
1. [HIGH] Set `BLOB_READ_WRITE_TOKEN` in Vercel prod — avatar upload returns 503 until then (env-only; not code-fixable).
2. [HIGH] Add IP-keyed rate limiting to the credentials login path (`[...nextauth]`) to complement tonight's per-email backoff; and add a production startup assertion that Upstash env is set so `checkRateLimit` cannot silently fail open.
3. [MEDIUM] Add a `/settings/profile` link in `Navigation.tsx` (beside the existing Privacy links) — the route is direct-URL-only today, same as `/settings/notifications`.
4. [MEDIUM] Normalize email (`.trim().toLowerCase()`) on write and every lookup across the auth surface (`authorize`, signup, reset-password) to eliminate case-variant duplicate accounts. Hash reset/beta-init tokens in `VerificationToken`.
5. [MEDIUM] Carried: unique `username`/handle column for deterministic @mentions (Day 11); apply `onboardedAt` to Neon prod (Day 7).
6. [LOW] Wire the dead `Avatar.tsx` / `AvatarStack.tsx` primitives into the surfaces that hand-roll avatar markup; add an attendee-avatar stack to the meetups list (needs the list endpoint to return attendee users, currently `_count` only).

These will be picked up by tomorrow's nightly (Day 13 — Heatmap polish + GDPR deletion).

## Errors & Warnings
- The concurrent baseline test run (kicked off before Wave 1) showed 6 transient failures that were entirely the 5 `users.test.ts` old-contract cases (fixed by [T3]) plus timing; the isolated re-run and final full-suite run were both 100% green.
- Recurring lesson reconfirmed: vitest (esbuild) does NOT full-typecheck. A test spreading a `Uint8Array` (`[...bytes]`) passed vitest but was tsc-RED (TS2802, no `downlevelIteration`); caught by the mandatory post-Wave `tsc --noEmit` and fixed with `Array.from()`. Test authors: do not spread typed arrays in this repo.
- Notion MCP unavailable in this non-interactive session (OAuth) — report saved locally only.

---
*Generated by OutTheGroupchat Nightly Build Coordinator v2 — plan-driven (BUILD_PLAN.md Day 12)*
